create database collage;
use collage;
create table teacher(
id int primary key,
name varchar(100) not null,
subject varchar(50) not null,
salary int 
);
insert into teacher 
values
(23,"ajay","math",50000),
(47,"Bharat","english",60000),
(18,"chetan","chemistry",45000),
(9,"divya","physics",75000);

select name as teacher from teacher where salary>=55000;


alter table teacher 
rename column subject to ctc;

set sql_safe_updates = 0;


select * from teacher;

update teacher 
set salary = salary + salary*25/100
where id>=1;
select * from teacher;

alter table teacher 
add column city varchar(100) default "gurgoan";

alter table teacher 
drop column ctc;

update teacher 
set salary = 75000
where name = "divya";
update teacher 
set salary = salary + salary*25/100
where id>=1;


select * from teacher;

